import * as React from 'react';
import { FlexBox } from '@tidy-ui/layout';
import { Alert, AlertFooter, AlertTitle, Button, ButtonCluster, Text } from '@tidy-ui/presentation';

const ReactLiveScope = {
  Alert,
  AlertFooter,
  AlertTitle,
  Button,
  ButtonCluster,
  FlexBox,
  React,
  Text,
};

export default ReactLiveScope;
