import * as React from 'react';
import { Alert, AlertFooter, AlertTitle, Button, ButtonCluster, FlexBox, Text } from '@tidy-ui/all';

const ReactLiveScope = {
  Alert,
  AlertFooter,
  AlertTitle,
  Button,
  ButtonCluster,
  FlexBox,
  React,
  Text,
};

export default ReactLiveScope;
